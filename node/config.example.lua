config = {
	--The full filepath pointing to the root of the domain. IE /var/www/rtdownloader
	www_dir = ""

--	this corresponds to whatever url is used to access the local webserver.
	local_url = "https://example.com";

--	this corresponds to wherever the backend is running.
	remote_url= "http://127.0.0.1:8000/api/";

--	This username and password is a login for a roosterteeth login account.
	username = "";
	password = "";
}